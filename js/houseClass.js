function House(score, address) {
        //this.picture = picture;
        this.score = score;
        this.address = address;
        //this.amenities.apply(this, am);
    };
    
    House.prototype.fillArray = function () {
        //figure out how to fill array with strings from Maps API
        console.log("asdf");
    }; 

    /*House.prototype.getAddress = function () {
        return address;
    }
   House.prototype.printThis = function () {
       document.write("mine" + markers[0][3]);
   } 
    */
   
    
  /*  for(var j in arrayforMarkers )
    {
        for(var p = 0; p < arrayforMarkers[j].length; p++ )
        {
             console.log(arrayforMarkers[j][p]);
        
        }
    }
    */
    
    
    